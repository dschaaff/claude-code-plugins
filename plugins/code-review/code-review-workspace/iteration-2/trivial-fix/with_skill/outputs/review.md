**Lightweight review** -- documentation-only change, single-line comment fix.

The staged diff adds a new `README.md` (22 lines) and a `README.md.patch` file explaining the intent. The comment on the `capitalize()` usage example shows `// "Hello World"` but `capitalize` only uppercases the first letter of the string, not each word. Changing it to `// "Hello world"` correctly reflects the function's behavior. The fix is correct. No issues.
